Сборка:
 ghc Main.hs -o compiler (уже должен быть собранный файл под линукс x86)

Использование:
 ./compiler <source.lisp

Синтаксис лиспа простой - можно вызывать функции:
(function arg1 arg2 arg3)

чтобы объявить новую функцию пишем:
(defun function (arg1 arg2 arg3 ...) (body))

Комментарии после ;

например:

(+ 1 (* 2 3)) ; hello

Поддерживаются стандартные функции:
+, -, *, /, >, <, =, >=, <=, atom, cons, car, cdr, clos
Функции типо +, -, * пока работают только для двух аргументов

Функция clos - Указатель на функцию по имени (нужно для возврата функции шага из пакмана)
 
(defun hello ...)

(defun main () (clos hello))
 
Есть поддержка if'ов: (if (= 5 5) (+ 3 4) (+ 8 9))

Для примеров кода смотри test*.lisp

*** Если хочется что-то пофиксить или добавить ***
то смотрите функцию generate' в файле compiler.hs - она единственная, в которой можно разобраться
 